parameters:
    foo: bar
    values:
        - true
        - false
        - 0
        - 1000.3
        - !Ref INT_MAX
    bar: foo
    escape: '@@escapeme'
    foo_bar: '@foo_bar'
    mixedcase:
        MixedCaseKey: value
